#include<stdio.h>
#include "0-object_like_macro.h"
#include "0-object_like_macro.h"

/**
 * main - checks the main header for the DEFINE
 * Return: Alway 1 for success and 0 otherwise
 */

int main(void)
{
int num = 40;

num += SIZE;

printf("The num plus the SIZE is %d", num);
putchar('\n');

return (0);
}
