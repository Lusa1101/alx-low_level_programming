#include "main.h"

/**
 * _atoi - function to convert a string to integer
 * @s: the string to be converted into an integer
 * Return: Nothing
 */

int _atoi(char *s)
{
int i = 0;
char str;

while (s[i] != '\0')
{

int j = 0;

if (s[i] / 1 == s[i])
{
str[j] = s[i];
j++;
}
}

return (str);
}
