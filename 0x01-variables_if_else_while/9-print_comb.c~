#include<stdio.h>

/**
 * main - prints all possible single digit combination
 *
 * Return: Always 0 
 */
int main(void)
{
int n = 0;

while (n < 10)
{
putchar(n % 10 + '0');
putchar(',');
putchar(' ');
if (n == 9)
break;
n++;
}
putchar('\n');
return (0);
}
      
